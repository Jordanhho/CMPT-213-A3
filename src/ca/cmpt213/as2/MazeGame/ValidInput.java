package ca.cmpt213.as2.MazeGame;

/**
 * ValidInput enum standardizes user inputs.
 */
public enum ValidInput {
    UP, DOWN, LEFT, RIGHT, MAP, HELP, INVALID, PASS, FREEZE, EXIT, RESTART, RADIUSUP, RADIUSDOWN, CHEESE, DIFFICULTY, YES, NO
}