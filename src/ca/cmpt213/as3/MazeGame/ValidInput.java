package ca.cmpt213.as3.MazeGame;

/**
 * ValidInput enum standardizes user inputs.
 */
public enum ValidInput {
    UP, DOWN, LEFT, RIGHT, MAP, HELP, INVALID, PASS, FREEZE, EXIT, RESTART, RADIUSUP, RADIUSDOWN, CHEESE, DIFFICULTY, YES, NO, START
}